# Базовый образ n8n (официальный).
# Расширяйте его при необходимости (доп. пакеты, шрифты, ffmpeg и т.п.).
FROM n8nio/n8n:latest

# Таймзона задаётся через переменную окружения TZ в docker-compose.yml,
# отдельная установка пакетов не требуется.
